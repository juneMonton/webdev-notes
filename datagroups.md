## Terminal commands

### Testing
- npx jest -runs the test tool (Jest) that reads filename.test.js and checks your code (Runs all test files it can find in the folder)
- npx jest filename.test.js -Runs only that specific test file
